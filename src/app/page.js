import HomeClient from "@/components/Home/HomeClient";

export const metadata = {
  title: 'Minat Matematika | Platform Latihan Soal UTBK SNBT',
};

export default function HomePage() {
  return (
    <main>
      <HomeClient />
    </main>
  );
}